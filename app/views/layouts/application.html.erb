<!DOCTYPE html>
<html>
<head>
  <title>Splurty</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div class="col-xs-2">
      <%= link_to root_path, class:'logo' do %>
      <div class="circle">
        <br />
        WE
        <br />
        &hearts;
        <br />
        YORKIES
      </div>
      <% end %>
    </div>
    <div class="col-xs-7 slogan hidden-xs">
      All the reasons that make us <span class="next-quote">&hearts;</span> Yorkies
    </div>
    <div class="col-xs-8 col-xs-offset-1 col-sm-offset-0 col-sm-3 links">
      <%= link_to 'Your Reason', '#', data:{toggle: 'modal', target: '#myModal'} %>
      <%= link_to 'About', about_path %>
    </div>
  </header>


  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel"><h1>Tell us why you <span class='next-quote'>&hearts;</span> yorkies</h1></h4>
        </div>
        <%= simple_form_for Quote.new do |f| %>
          <div class="modal-body">
            <%= f.input :saying, label: 'Reason', placeholder: 'Besides the fact that Yorkies are basically just awesome...'  %>
            <br />
            <%= f.input :author, label: 'Your name', placeholder:'Yokie lover' %>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Share your love!', class: 'btn btn-danger' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <hr />

  <br class="clear">

  <div class="main-area">
    <div class="container">
      <% if flash[:error].present? %>
        <div class="col-xs-10 col-xs-offset-1 alert alert-danger"><%= flash[:error].html_safe %></div>
      <% end %>
      <%= yield %>
    </div>
  </div>

  <footer class="footer">
    <div class="container">
      <div class="col-sm-8 col-xs-12 footer-link"><%= link_to 'Show me another reason', root_path, class: 'next-quote' %></div>
      <div class="col-sm-4 col-xs-12 footer-link made-by">made by <a href="mailto:arwen0603@gmail.com">Celia</a> with <span class="next-quote">&hearts;</span></div>
    </div>
  </footer>

</body>
</html>
